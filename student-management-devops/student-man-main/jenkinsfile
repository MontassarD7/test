pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('student-management-devops/student-man-main') {
                    sh 'mvn clean install'
                }
            }
        }

        stage('Test') {
            steps {
                dir('student-management-devops/student-man-main') {
                    sh 'mvn test'
                }
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying...'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                dir('student-management-devops/student-man-main') {
                    withSonarQubeEnv('SonarQube') {
                        script {
                            def mavenHome = tool name: "Maven", type: "maven"
                            sh "${mavenHome}/bin/mvn clean package sonar:sonar -DskipTests"
                        }
                    }
                }
            }
        }
    }
}
